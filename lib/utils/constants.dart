import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Colors.blue; // Use Colors.blue instead of Color.blue
  static const secondaryColor = Colors.green; // Use Colors.green instead of Color.green
}

class AppStrings {
  static const appName = "Hostel Manager";
}

